{
  "name": "@danprince/games",
  "main": "dist/index.umd.js",
  "module": "dist/index.es.js",
  "types": "dist/index.d.ts",
  "version": "0.0.0",
  "files": ["dist"],
  "scripts": {
    "dev": "vite",
    "build": "vite build && tsc",
    "typecheck": "tsc && tsc -p examples && tsc -p tests",
    "preview": "vite preview",
    "test": "vitest",
    "prepublishOnly": "yarn typecheck && yarn test && yarn build"
  },
  "devDependencies": {
    "canvas": "^2.9.3",
    "fast-glob": "^3.2.11",
    "jsdom": "^20.0.0",
    "typescript": "^4.5.4",
    "vite": "^2.9.9",
    "vitest": "^0.17.0"
  }
}
